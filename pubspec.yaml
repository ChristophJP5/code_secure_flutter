name: code_secure_flutter
description: "A Set of rules to ensure secure coding practices in Flutter applications."
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  analyzer: ^7.0.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.7.3
  collection: 

dev_dependencies:
  flutter_lints: ^6.0.0
  custom_lint: ^0.7.3

